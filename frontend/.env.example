# Dev: use Vite proxy (see vite.config.js) so the UI talks to the same origin.
VITE_API_BASE=/api/v1

# If you prefer direct calls to FastAPI (no proxy), use e.g.:
# VITE_API_BASE=http://127.0.0.1:8000/api/v1
